# 树数据结构详解

## 目录
1. [树的基本概念](#树的基本概念)
2. [树的术语和定义](#树的术语和定义)
3. [二叉树](#二叉树)
4. [二叉树的遍历](#二叉树的遍历)
5. [特殊二叉树](#特殊二叉树)
6. [树的存储结构](#树的存储结构)
7. [树的应用](#树的应用)
8. [算法复杂度分析](#算法复杂度分析)
9. [实现示例](#实现示例)

---

## 树的基本概念

### 什么是树？

树（Tree）是一种**非线性数据结构**，由n（n≥0）个有限节点组成一个具有层次关系的集合。树具有以下特点：

1. **有且仅有一个根节点**：树中唯一没有前驱的节点
2. **层次结构**：除根节点外，每个节点都有且仅有一个前驱
3. **分支结构**：每个节点可以有零个或多个后继
4. **无环结构**：树中不存在环路

### 树的形象比喻

树就像现实中的树一样：
- **根节点**：树的根部
- **分支节点**：树干的分叉
- **叶子节点**：树的叶子
- **层次**：从根到叶子的不同高度

---

## 树的术语和定义

### 基本术语

| 术语 | 定义 | 示例 |
|------|------|------|
| **节点（Node）** | 树中的基本单位，包含数据和指针 | 每个圆圈代表一个节点 |
| **根节点（Root）** | 树的顶端节点，没有父节点 | 节点A |
| **父节点（Parent）** | 某个节点的直接前驱 | B的父节点是A |
| **子节点（Child）** | 某个节点的直接后继 | A的子节点是B、C |
| **兄弟节点（Sibling）** | 具有相同父节点的节点 | B和C是兄弟节点 |
| **叶子节点（Leaf）** | 没有子节点的节点 | D、E、F、G |
| **内部节点（Internal）** | 有子节点的节点 | A、B、C |

### 树的属性

#### 1. 度（Degree）
- **节点的度**：节点拥有的子树个数
- **树的度**：树中所有节点度的最大值

#### 2. 层次和深度
- **层次（Level）**：从根节点开始，根为第1层
- **深度（Depth）**：树中节点的最大层次数
- **高度（Height）**：从某节点到叶子节点的最长路径

#### 3. 路径和路径长度
- **路径**：从节点A到节点B的节点序列
- **路径长度**：路径上边的数量

---

## 二叉树

### 二叉树的定义

二叉树（Binary Tree）是每个节点最多有两个子树的树结构，通常子树被称作"左子树"和"右子树"。

### 二叉树的特点

1. **每个节点最多有两个子节点**
2. **子节点有左右之分**：左子树和右子树
3. **左子树和右子树也是二叉树**

### 二叉树的五种基本形态

```
1. 空树：没有节点
2. 只有根节点：没有子树
3. 只有左子树：根节点只有左子节点
4. 只有右子树：根节点只有右子节点
5. 左右子树都有：根节点有两个子节点
```

### 二叉树的性质

#### 性质1：第i层最多有2^(i-1)个节点
- 第1层：1个节点
- 第2层：2个节点
- 第3层：4个节点
- 第i层：2^(i-1)个节点

#### 性质2：深度为k的二叉树最多有2^k-1个节点
- 深度1：1个节点
- 深度2：3个节点
- 深度3：7个节点
- 深度k：2^k-1个节点

#### 性质3：叶子节点数 = 度为2的节点数 + 1
- 设叶子节点数为n₀，度为1的节点数为n₁，度为2的节点数为n₂
- 则：n₀ = n₂ + 1

---

## 二叉树的遍历

### 遍历的定义

遍历（Traversal）是指按照某种规则访问树中的每个节点，且每个节点只访问一次。

### 三种基本遍历方式

#### 1. 前序遍历（Preorder Traversal）
**访问顺序**：根 → 左 → 右

```cpp
void preorder(TreeNode* root) {
    if (root == nullptr) return;
    
    visit(root);           // 访问根节点
    preorder(root->left);  // 遍历左子树
    preorder(root->right); // 遍历右子树
}
```

**应用场景**：
- 复制二叉树
- 计算目录大小
- 表达式树求值

#### 2. 中序遍历（Inorder Traversal）
**访问顺序**：左 → 根 → 右

```cpp
void inorder(TreeNode* root) {
    if (root == nullptr) return;
    
    inorder(root->left);   // 遍历左子树
    visit(root);           // 访问根节点
    inorder(root->right);  // 遍历右子树
}
```

**应用场景**：
- 二叉搜索树的中序遍历得到有序序列
- 表达式树的中序遍历得到中缀表达式

#### 3. 后序遍历（Postorder Traversal）
**访问顺序**：左 → 右 → 根

```cpp
void postorder(TreeNode* root) {
    if (root == nullptr) return;
    
    postorder(root->left);  // 遍历左子树
    postorder(root->right); // 遍历右子树
    visit(root);            // 访问根节点
}
```

**应用场景**：
- 删除二叉树
- 计算目录大小
- 表达式树求值

### 层序遍历（Level Order Traversal）

**访问顺序**：按层次从上到下，从左到右

```cpp
void levelOrder(TreeNode* root) {
    if (root == nullptr) return;
    
    queue<TreeNode*> q;
    q.push(root);
    
    while (!q.empty()) {
        TreeNode* node = q.front();
        q.pop();
        
        visit(node);
        
        if (node->left) q.push(node->left);
        if (node->right) q.push(node->right);
    }
}
```

---

## 特殊二叉树

### 1. 满二叉树（Full Binary Tree）

**定义**：每一层的节点数都达到最大值，即第i层有2^(i-1)个节点。

**特点**：
- 深度为k的满二叉树有2^k-1个节点
- 叶子节点都在最后一层
- 内部节点都有两个子节点

### 2. 完全二叉树（Complete Binary Tree）

**定义**：除了最后一层，其他层都是满的，且最后一层的节点都靠左排列。

**特点**：
- 可以用数组存储
- 父节点索引为i，左子节点为2i+1，右子节点为2i+2
- 常用于堆的实现

### 3. 二叉搜索树（Binary Search Tree, BST）

**定义**：对于任意节点，其左子树所有节点值小于该节点值，右子树所有节点值大于该节点值。

**特点**：
- 中序遍历得到有序序列
- 查找、插入、删除的平均时间复杂度为O(log n)
- 最坏情况下退化为链表，时间复杂度为O(n)

### 4. 平衡二叉树（AVL Tree）

**定义**：任意节点的左右子树高度差不超过1的二叉搜索树。

**特点**：
- 保证查找、插入、删除的时间复杂度为O(log n)
- 通过旋转操作维护平衡性

### 5. 红黑树（Red-Black Tree）

**定义**：一种自平衡二叉搜索树，通过颜色标记维护平衡。

**特点**：
- 最长路径不超过最短路径的2倍
- 插入、删除、查找的时间复杂度为O(log n)
- 广泛应用于C++的std::map和std::set

---

## 树的存储结构

### 1. 链式存储结构

```cpp
struct TreeNode {
    int data;
    TreeNode* left;
    TreeNode* right;
    
    TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}
};
```

**优点**：
- 结构清晰，易于理解
- 动态分配内存，节省空间
- 便于插入和删除操作

**缺点**：
- 指针占用额外空间
- 内存分配不连续，缓存性能较差

### 2. 数组存储结构

```cpp
// 完全二叉树的数组表示
int tree[MAX_SIZE];
// 根节点：tree[0]
// 父节点i的左子节点：tree[2*i+1]
// 父节点i的右子节点：tree[2*i+2]
```

**优点**：
- 内存连续，缓存性能好
- 访问速度快
- 节省指针空间

**缺点**：
- 只适用于完全二叉树
- 插入删除操作复杂

### 3. 三叉链表存储

```cpp
struct TreeNode {
    int data;
    TreeNode* parent;  // 父节点指针
    TreeNode* left;
    TreeNode* right;
};
```

**优点**：
- 可以快速找到父节点
- 便于某些算法的实现

**缺点**：
- 占用更多内存空间

---

## 树的应用

### 1. 文件系统

```
根目录/
├── 用户/
│   ├── 文档/
│   │   ├── 报告.docx
│   │   └── 图片/
│   └── 下载/
└── 系统/
    └── 配置/
```

### 2. 表达式树

```
表达式：a + b * c
树结构：
    +
   / \
  a   *
     / \
    b   c
```

### 3. 决策树

```
天气决策树：
    天气
   /    \
晴天     雨天
/  \     /  \
去公园  带伞  在家
```

### 4. 数据库索引

- B树和B+树用于数据库索引
- 提高数据检索效率

### 5. 编译器

- 语法分析树
- 抽象语法树（AST）

---

## 算法复杂度分析

### 时间复杂度

| 操作 | 一般二叉树 | 平衡二叉树 | 二叉搜索树 |
|------|------------|------------|------------|
| 查找 | O(n) | O(log n) | O(log n) |
| 插入 | O(n) | O(log n) | O(log n) |
| 删除 | O(n) | O(log n) | O(log n) |
| 遍历 | O(n) | O(n) | O(n) |

### 空间复杂度

| 存储方式 | 空间复杂度 | 说明 |
|----------|------------|------|
| 链式存储 | O(n) | n个节点，每个节点3个指针 |
| 数组存储 | O(n) | n个节点，每个节点1个数据 |
| 递归遍历 | O(h) | h为树的高度，递归栈空间 |

---

## 实现示例

### 完整的二叉树实现

```cpp
#include <iostream>
#include <queue>
#include <vector>
using namespace std;

class BinaryTree {
private:
    struct TreeNode {
        int data;
        TreeNode* left;
        TreeNode* right;
        
        TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}
    };
    
    TreeNode* root;
    
public:
    BinaryTree() : root(nullptr) {}
    
    // 插入节点
    void insert(int data) {
        root = insertRecursive(root, data);
    }
    
    TreeNode* insertRecursive(TreeNode* node, int data) {
        if (node == nullptr) {
            return new TreeNode(data);
        }
        
        if (data < node->data) {
            node->left = insertRecursive(node->left, data);
        } else if (data > node->data) {
            node->right = insertRecursive(node->right, data);
        }
        
        return node;
    }
    
    // 前序遍历
    vector<int> preorderTraversal() {
        vector<int> result;
        preorderHelper(root, result);
        return result;
    }
    
    void preorderHelper(TreeNode* node, vector<int>& result) {
        if (node == nullptr) return;
        
        result.push_back(node->data);
        preorderHelper(node->left, result);
        preorderHelper(node->right, result);
    }
    
    // 中序遍历
    vector<int> inorderTraversal() {
        vector<int> result;
        inorderHelper(root, result);
        return result;
    }
    
    void inorderHelper(TreeNode* node, vector<int>& result) {
        if (node == nullptr) return;
        
        inorderHelper(node->left, result);
        result.push_back(node->data);
        inorderHelper(node->right, result);
    }
    
    // 后序遍历
    vector<int> postorderTraversal() {
        vector<int> result;
        postorderHelper(root, result);
        return result;
    }
    
    void postorderHelper(TreeNode* node, vector<int>& result) {
        if (node == nullptr) return;
        
        postorderHelper(node->left, result);
        postorderHelper(node->right, result);
        result.push_back(node->data);
    }
    
    // 层序遍历
    vector<int> levelOrderTraversal() {
        vector<int> result;
        if (root == nullptr) return result;
        
        queue<TreeNode*> q;
        q.push(root);
        
        while (!q.empty()) {
            TreeNode* node = q.front();
            q.pop();
            
            result.push_back(node->data);
            
            if (node->left) q.push(node->left);
            if (node->right) q.push(node->right);
        }
        
        return result;
    }
    
    // 查找节点
    bool search(int data) {
        return searchHelper(root, data);
    }
    
    bool searchHelper(TreeNode* node, int data) {
        if (node == nullptr) return false;
        if (node->data == data) return true;
        
        return searchHelper(node->left, data) || searchHelper(node->right, data);
    }
    
    // 计算树的高度
    int height() {
        return heightHelper(root);
    }
    
    int heightHelper(TreeNode* node) {
        if (node == nullptr) return 0;
        
        int leftHeight = heightHelper(node->left);
        int rightHeight = heightHelper(node->right);
        
        return max(leftHeight, rightHeight) + 1;
    }
    
    // 计算节点数
    int countNodes() {
        return countNodesHelper(root);
    }
    
    int countNodesHelper(TreeNode* node) {
        if (node == nullptr) return 0;
        
        return 1 + countNodesHelper(node->left) + countNodesHelper(node->right);
    }
    
    // 打印树结构
    void printTree() {
        printTreeHelper(root, 0);
    }
    
    void printTreeHelper(TreeNode* node, int depth) {
        if (node == nullptr) return;
        
        for (int i = 0; i < depth; i++) {
            cout << "  ";
        }
        cout << node->data << endl;
        
        printTreeHelper(node->left, depth + 1);
        printTreeHelper(node->right, depth + 1);
    }
};

// 使用示例
int main() {
    BinaryTree tree;
    
    // 插入节点
    tree.insert(5);
    tree.insert(3);
    tree.insert(7);
    tree.insert(1);
    tree.insert(9);
    
    // 遍历
    cout << "前序遍历: ";
    vector<int> preorder = tree.preorderTraversal();
    for (int val : preorder) {
        cout << val << " ";
    }
    cout << endl;
    
    cout << "中序遍历: ";
    vector<int> inorder = tree.inorderTraversal();
    for (int val : inorder) {
        cout << val << " ";
    }
    cout << endl;
    
    cout << "后序遍历: ";
    vector<int> postorder = tree.postorderTraversal();
    for (int val : postorder) {
        cout << val << " ";
    }
    cout << endl;
    
    cout << "层序遍历: ";
    vector<int> levelorder = tree.levelOrderTraversal();
    for (int val : levelorder) {
        cout << val << " ";
    }
    cout << endl;
    
    // 其他操作
    cout << "树的高度: " << tree.height() << endl;
    cout << "节点数: " << tree.countNodes() << endl;
    cout << "查找节点3: " << (tree.search(3) ? "找到" : "未找到") << endl;
    
    return 0;
}
```

---

## 总结

树是一种非常重要的数据结构，具有以下特点：

### 优势
1. **层次结构清晰**：便于表示具有层次关系的数据
2. **查找效率高**：在平衡树中查找效率为O(log n)
3. **插入删除灵活**：支持动态插入和删除操作
4. **应用广泛**：在文件系统、数据库、编译器等领域有重要应用

### 学习重点
1. **理解树的基本概念**：节点、边、层次、深度等
2. **掌握遍历算法**：前序、中序、后序、层序遍历
3. **熟悉特殊二叉树**：完全二叉树、二叉搜索树、平衡树
4. **了解存储结构**：链式存储和数组存储的优缺点
5. **掌握应用场景**：文件系统、表达式树、决策树等

树数据结构是计算机科学的基础，掌握好树的概念和操作，对于学习更高级的数据结构和算法具有重要意义。
