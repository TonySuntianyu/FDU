# 数据结构 Lab4 实验报告
## 二叉树的构建与三种遍历实现

### 一、实验目的

本实验旨在通过编程实现二叉树的构建和三种基本遍历方法（前序、中序、后序），加深对二叉树数据结构的理解，掌握递归算法在树遍历中的应用。

### 二、问题分析

#### 2.1 题目要求
- 根据层序遍历序列构建二叉树
- 实现前序、中序、后序遍历
- 输出三种遍历的结果

#### 2.2 核心挑战
1. **层序遍历序列解析**：需要正确解析包含"null"的层序遍历序列
2. **二叉树构建**：根据层序遍历序列重建二叉树结构
3. **三种遍历实现**：实现递归的前序、中序、后序遍历算法

### 三、算法设计

#### 3.1 数据结构设计

```cpp
struct TreeNode {
    int val;           // 节点值
    TreeNode* left;    // 左子节点指针
    TreeNode* right;   // 右子节点指针
    
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};
```

**设计说明**：
- 使用结构体定义二叉树节点
- 包含节点值和两个子节点指针
- 构造函数初始化节点值，子节点指针设为nullptr

#### 3.2 二叉树构建算法

```cpp
TreeNode* buildTree(vector<string>& levelOrder) {
    if (levelOrder.empty() || levelOrder[0] == "null") {
        return nullptr;
    }
    
    TreeNode* root = new TreeNode(stoi(levelOrder[0]));
    queue<TreeNode*> q;
    q.push(root);
    
    int i = 1;
    while (!q.empty() && i < levelOrder.size()) {
        TreeNode* node = q.front();
        q.pop();
        
        // 处理左子节点
        if (i < levelOrder.size()) {
            if (levelOrder[i] != "null") {
                node->left = new TreeNode(stoi(levelOrder[i]));
                q.push(node->left);
            }
            i++;
        }
        
        // 处理右子节点
        if (i < levelOrder.size()) {
            if (levelOrder[i] != "null") {
                node->right = new TreeNode(stoi(levelOrder[i]));
                q.push(node->right);
            }
            i++;
        }
    }
    
    return root;
}
```

**算法原理**：
1. **队列辅助**：使用队列存储待处理节点，保证按层序处理
2. **双指针遍历**：用索引i遍历层序遍历序列
3. **节点创建**：遇到非"null"值时创建新节点
4. **子节点连接**：将新节点连接到父节点的左或右子节点

**时间复杂度**：O(n)，其中n为节点数
**空间复杂度**：O(n)，队列和树的空间开销

#### 3.3 三种遍历算法

##### 3.3.1 前序遍历（根→左→右）

```cpp
void preorderTraversal(TreeNode* root, vector<int>& result) {
    if (root == nullptr) return;
    
    result.push_back(root->val);        // 访问根节点
    preorderTraversal(root->left, result);   // 遍历左子树
    preorderTraversal(root->right, result); // 遍历右子树
}
```

##### 3.3.2 中序遍历（左→根→右）

```cpp
void inorderTraversal(TreeNode* root, vector<int>& result) {
    if (root == nullptr) return;
    
    inorderTraversal(root->left, result);    // 遍历左子树
    result.push_back(root->val);             // 访问根节点
    inorderTraversal(root->right, result);   // 遍历右子树
}
```

##### 3.3.3 后序遍历（左→右→根）

```cpp
void postorderTraversal(TreeNode* root, vector<int>& result) {
    if (root == nullptr) return;
    
    postorderTraversal(root->left, result);   // 遍历左子树
    postorderTraversal(root->right, result);   // 遍历右子树
    result.push_back(root->val);               // 访问根节点
}
```

**递归原理**：
- **递归终止条件**：节点为空时返回
- **递归过程**：按照特定顺序访问根节点、左子树、右子树
- **结果收集**：将访问的节点值存入结果向量

### 四、输入输出处理

#### 4.1 输入解析

```cpp
string input;
getline(cin, input);

vector<string> levelOrder;
string token;
for (char c : input) {
    if (c == ' ') {
        if (!token.empty()) {
            levelOrder.push_back(token);
            token.clear();
        }
    } else {
        token += c;
    }
}
if (!token.empty()) {
    levelOrder.push_back(token);
}
```

**解析过程**：
1. 读取整行输入
2. 按空格分割字符串
3. 将分割后的字符串存入向量

#### 4.2 输出格式化

```cpp
void printResult(const vector<int>& result) {
    for (int i = 0; i < result.size(); i++) {
        if (i > 0) cout << " ";
        cout << result[i];
    }
    cout << endl;
}
```

**输出特点**：
- 节点值用空格分隔
- 每行输出一种遍历结果
- 格式符合题目要求

### 五、测试验证

#### 5.1 测试用例1

**输入**：`1 null 2 3`

**树结构**：
```
    1
     \
      2
     /
    3
```

**遍历结果**：
- 前序遍历：1 2 3
- 中序遍历：1 3 2  
- 后序遍历：3 2 1

#### 5.2 测试用例2

**输入**：`1 2 3 null 5 6 null 8 9`

**树结构**：
```
        1
       / \
      2   3
       \   \
        5   6
       / \
      8   9
```

**遍历结果**：
- 前序遍历：1 2 5 8 9 3 6
- 中序遍历：2 8 5 9 1 6 3
- 后序遍历：8 9 5 2 6 3 1

### 六、算法复杂度分析

#### 6.1 时间复杂度
- **构建二叉树**：O(n)，每个节点访问一次
- **三种遍历**：O(n)，每个节点访问一次
- **总体复杂度**：O(n)

#### 6.2 空间复杂度
- **二叉树存储**：O(n)
- **队列空间**：O(w)，w为树的最大宽度
- **递归栈空间**：O(h)，h为树的高度
- **总体复杂度**：O(n)

### 七、代码特点

#### 7.1 优点
1. **算法正确性**：严格按照遍历定义实现
2. **代码可读性**：清晰的注释和变量命名
3. **结构清晰**：功能模块化，易于理解和维护
4. **边界处理**：正确处理空节点和边界情况

#### 7.2 改进空间
1. **内存管理**：可添加析构函数释放内存
2. **错误处理**：可增加输入格式验证
3. **性能优化**：对于大数据量可考虑迭代实现

### 八、实验总结

本实验成功实现了二叉树的构建和三种遍历算法，通过递归方法简洁地实现了复杂的树遍历逻辑。实验加深了对二叉树数据结构的理解，掌握了递归算法在树操作中的应用，为后续学习更复杂的树算法奠定了基础。

**主要收获**：
1. 掌握了二叉树的构建方法
2. 理解了三种遍历的递归实现
3. 学会了队列在树构建中的应用
4. 提高了递归算法的编程能力

**技术要点**：
- 层序遍历序列的解析和树构建
- 递归遍历算法的实现
- 队列数据结构的应用
- 字符串处理和输入输出格式化
